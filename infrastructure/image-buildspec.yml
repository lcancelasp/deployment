version: 0.2
phases:
  build:
    commands:
      - docker build -f infrastructure/Dockerfile -t serverless-app .
  post_build:
    commands:
      - eval $(aws ecr get-login --region eu-west-1 --registry-ids 193006638607) && docker tag serverless-app:latest 193006638607.dkr.ecr.eu-west-1.amazonaws.com/serverless-app:latest && docker push 193006638607.dkr.ecr.eu-west-1.amazonaws.com/serverless-app:latest

### eval $(aws ecr get-login --registry-ids 193006638607) && 

