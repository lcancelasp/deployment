
version: 0.2

phases:
  build:
    commands:
      - docker build -f infrastructure/Dockerfile -t maven_image .
  post_build:
    commands:
      - eval $(aws ecr get-login --registry-ids 193006638607) && docker push maven_image 
